#include "main.h"
#include <stdio.h>
#include <string.h>
/**
*_strcpy - check the code
*@dest: pointer
*@src: variable
*Return: Always 0.
*/
char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0; i < src[i]; i++)
	{
		dest[i] = src[i];
	}
	return ("\n");
	return (dest);
}
