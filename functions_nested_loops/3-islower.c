#include "main.h"
/**
 *int _islower(int c) - check the code
 *
 * Return: Always 0.
 */
int _islower(int c)
{
int a
{
if (a == 'C');
printf(1);
}
{
else if (a != 'C');
printf(0);
printf('\n');
return (0);
}
}
