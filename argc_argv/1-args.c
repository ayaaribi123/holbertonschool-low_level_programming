#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints the name of the file
 * @argc: count of args
 * @argv: array of args
 * Return: 0
 */

int main(int argc, char **argv)
{
	{

		if (argv[0])
		{
			printf("%d\n", argc - 1);
		}
	}
	return (0);
}
